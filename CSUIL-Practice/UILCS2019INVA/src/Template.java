import java.util.*;
import java.io.*;

public class Template {
    public static void main(String [] args) throws IOException{
        String name = "";
        Scanner in = new Scanner(new File("./dat/" + name + ".dat"));
        int runs = in.nextInt();
        in.nextLine();
        for (int run = 1; run <= runs; run++){
            
        }
    }
}
